spring:
  application:
    name: prometeo

  datasource:
    url: jdbc:postgresql://localhost:5432/apolo
    username: postgres
    password: drakotako1234
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
